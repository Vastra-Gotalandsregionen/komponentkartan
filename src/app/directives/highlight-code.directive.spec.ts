import { HighlightCodeDirective } from './highlight-code.directive';

describe('HighlightCodeDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightCodeDirective();
    expect(directive).toBeTruthy();
  });
});
